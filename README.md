# Circular-Supply-Chain-Performance-Indicator
 The project generates a set of key performance indicators in four dimensions: operational efficiency, environmental impact (proxy indicators), circularity (exception and loss indicators) and resilience, to be normalised and then aggregated into a composite index in VS Code using Python (Pandas, Numpy, Matplotlib).
